# v2.1 新版代码Bug、逻辑缺陷、隐性风险完整梳理
相比 v2.0 修复了大量问题：单实例锁、窗口可缩放、移除单选框冲突加速键、支持权重0屏蔽抽取、CSV批量权重修正汇总弹窗、高DPI适配、Ctrl+1/2/3切换模式、表格列自适应等，但仍存在**致命功能Bug、边界逻辑缺陷、UI交互瑕疵、文件并发隐患、代码设计短板**四类问题，按优先级排序：

## 一、高优先级：会导致功能异常/数据丢失/崩溃的Bug
### 1. 单实例锁 `QSharedMemory` 程序异常退出时不会自动释放，进程卡死无法二次打开
#### 问题
```python
shared_mem = QSharedMemory("ClassRandomSampler_Instance_2.1")
if shared_mem.attach():
    弹窗退出
shared_mem.create(1)
```
正常关闭程序会销毁QSharedMemory、释放共享内存；但程序崩溃、强制杀进程、断电时，共享内存残留在系统，下次启动直接提示“程序已运行”，无法打开，只能重启电脑或手动清理共享内存。
#### 复现：任务管理器强制结束程序，再次双击打开。

### 2. 多进程同时写日志无文件锁，日志错乱、内容截断丢失
`_log_operation` 直接 `open(..., "a")` 追加，多开程序（绕过单实例锁漏洞/多用户登录）同时抽取，日志行穿插、半截记录。
单实例锁仅限制**同一用户同一程序**单开，多Windows账户可同时运行，日志冲突必现。

### 3. 日志满1000条自动删除最早日志，无任何弹窗/日志记录告知用户
达到 `MAX_LOG_FILES=1000` 后静默删除旧日志，历史抽取记录永久丢失，没有任何提示，教师重要课堂记录无声消失。

### 4. 关闭窗口 `closeEvent` 保存不生成备份，异常断电会丢失本次修改
```python
def closeEvent(self, event):
    self._save_data(backup=False)
```
只有手动导入CSV、修改权重、重置权重时才生成 `.bak` 备份；直接关闭窗口的修改不备份。若关机断电、系统崩溃，本次修改无回滚渠道。

### 5. 权重弹窗永远加载**全部学生**，不跟随当前性别筛选视图，交互误导
勾选「只抽女生」打开修改权重弹窗，弹窗显示全班所有人。
虽然用学号匹配不会出现权重赋值错乱，但用户直观以为只修改当前筛选学生，操作逻辑割裂，极易误改不需要调整的学生权重。

### 6. CSV导入规整权重仅提示修正记录，但**不会写入操作日志**
批量权重修正弹窗展示调整内容，但没有把“哪些学号权重被自动修正”写入log日志，后续追溯导入操作无法查看权重变更历史。

### 7. 界面左侧表格会显示权重=0的学生，但抽取时自动过滤，极易造成认知混淆
`_get_filtered_students` 仅按性别过滤，表格展示所有权重0学生；但 `_draw_student` 内部二次过滤 `s["weight"]>0`。
场景：表格能看到某学生，点击抽取永远抽不到，新手用户会疑惑“名单里有为什么不抽取”，无视觉标记区分屏蔽学生。

## 二、中优先级：边界逻辑缺陷（特殊场景报错/逻辑不严谨）
### 1. 筛选后所有学生权重全部为0，校验重复执行两次
```python
filtered = [s for s in self._get_filtered_students() if s["weight"] > 0]
if not filtered:  # 第一层判断为空弹窗
    return
if not self._validate_weights(filtered): # 第二层再求和校验总权重>0
```
当过滤后列表非空，但是所有权重总和≤0（理论不可能，权重最小0.01），属于冗余校验；且逻辑分层混乱，可读性差。

### 2. 仅1名可抽取学生时，无提示告知用户抽取结果固定
筛选后只剩1个权重>0的学生，点击抽取直接固定选中此人，无弹窗提醒，用户会误以为随机失效。

### 3. 状态栏总权重数值过大时文字溢出截断，无数字格式化处理
大量学生权重总和上万后，状态栏文本超出永久标签可视宽度，看不到男女统计数字。

### 4. 日志文本框无清空、导出功能，日志无限堆积占用内存
上万条日志持续渲染在QTextEdit，内存持续上涨；无法一键导出抽取记录（教师需要导出做台账），只能手动去文件夹复制log。

### 5. 导入CSV超大名单时，权重修正弹窗最多只显示前10条，无完整记录留存
`weight_fixes[:10]` 截断展示，超过10条仅提示总数，完整修正清单没有保存到日志或本地文件，无法完整追溯。

### 6. 启动时仅校验一次目录写入权限，运行中途权限失效无阻断
中途磁盘满、文件夹被管理员设为只读，后续保存数据、写日志仅状态栏3秒红色一闪而过，操作无阻断，用户以为保存成功实际丢失数据。

## 三、UI/交互体验Bug
### 1. 日志写入失败红色提示仅3秒自动消失，无持久化错误记录
磁盘满、权限不足导致日志写入失败，红色状态栏提示3秒后自动恢复黑色，错过提示则完全不知道抽取记录未保存。

### 2. 无“清空抽取结果”按钮，只能切换性别筛选重置顶部姓名
用户不想保留上一轮红色抽取名字，必须切换筛选模式，操作繁琐。

### 3. 权重修改弹窗缺少批量操作功能：批量置0、批量统一权重
需要批量屏蔽一批学生、统一设权重时只能逐个修改SpinBox，效率极低。

### 4. 关于窗口文字不会自适应换行，窗口缩小后文字挤压重叠
`QMessageBox.about` 固定弹窗尺寸，长文本紧凑拥挤，阅读困难。

### 5. 导入CSV成功无完成弹窗，仅状态栏数字刷新，用户无法确认导入完成
导入大批量名册后，只能自己看状态栏学生总数判断是否导入成功，缺少明确完成提示。

### 6. 无记住窗口位置、大小功能，每次打开都恢复初始900*600
高分屏、多显示器场景每次启动都要手动拖拽调整窗口。

## 四、文件编码、导入兼容缺陷
### 1. CSV导入仅硬编码 `utf-8-sig`，不兼容Windows Excel默认GBK编码
Excel直接另存CSV默认GBK，导入中文姓名、性别全部乱码并解析失败，无自动编码探测/兼容兜底。

### 2. 日志删除失败仅 `except OSError: pass` 静默忽略，无告警
日志达到1000上限自动删除旧文件，文件被占用/只读时删除失败，程序持续循环尝试复用旧日志编号，隐性性能损耗，用户完全不知情。

## 五、代码设计与可维护性隐患
### 1. 全部提示文案、模式字符串、文件路径硬编码散落在代码，无统一常量管理
修改提示文字、文件后缀、抽取模式（all/male/female）需要全局检索，极易出现前后文案不一致。

### 2. 学生使用原生Dict存储，无数据模型类封装，裸key访问无静态校验
`s["id"]`/`s["weight"]` 分散全代码，后续新增字段极易漏处理，无IDE类型提示。

### 3. 男女统计、总权重每次刷新状态栏全量遍历求和，无缓存
几百上千学生频繁切换筛选、点击抽取时重复计算，轻微卡顿，缺少缓存变量。

### 4. 缺少菜单功能：导出抽取日志、手动备份名册、清空本地日志、批量管理学生
核心数据导出、备份功能缺失，全部依赖用户手动操作文件夹。

### 5. 硬耦合Windows专属 `APPDATA` 环境变量，无法跨平台（Linux/macOS直接报错）
未做跨平台数据目录兼容，非Windows系统启动直接崩溃。

## 六、低优先级小瑕疵（不影响核心功能）
1. 权重SpinBox步长固定0.50，无法自定义步长；
2. 无抽取次数统计、今日抽取计数展示；
3. 状态栏错误提示仅变色，无弹窗留存完整错误详情；
4. 日志时间戳毫秒固定截取3位，无配置开关；
5. 没有暗色模式/界面主题切换。

# v2.1 对比 v2.0 已修复的关键问题（总结）
✅ 增加单实例QSharedMemory锁，禁止重复打开同一程序
✅ 移除单选框Alt+&冲突快捷键，改用Ctrl+1/2/3切换模式
✅ 窗口支持缩放，设置最小尺寸800*500，适配不同屏幕
✅ 表格学号列自适应、姓名列拉伸，解决姓名截断问题
✅ 权重支持0，0代表不参与抽取，抽取逻辑自动过滤权重0学生
✅ CSV权重修正不再单行弹窗轰炸，统一汇总弹窗展示
✅ 开启Qt高DPI缩放，高分屏字体模糊问题修复
✅ 权重规整为0.5倍数，限制区间0.00~99.50，导入自动修正超限值
✅ 切换筛选自动重置顶部抽取文字，消除视觉误导
✅ 日志上限满后逻辑优化，避免无限死循环