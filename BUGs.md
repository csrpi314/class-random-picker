# v2.2 新版完整Bug、风险、边界缺陷分析
本次迭代改动：
1. 弃用`QSharedMemory`改用`QLockFile`文件锁解决异常残留锁问题；
2. 新增**自定义数据目录**功能，通过`QSettings`持久化；
3. 回归JSON单文件存储（取消上一版SQLite），保留bak备份、关闭自动保存；
4. 抽取结果同时展示学号+姓名；
5. 其余基础逻辑沿用v2.1。

下面分**致命阻断Bug、数据丢失风险、交互逻辑缺陷、虚拟机极限场景隐患、代码设计短板、已修复优化点**分类说明。

## 一、高优先级：致命阻断/崩溃Bug
### 1. QLockFile 锁文件路径硬编码死路径，切换自定义数据目录后锁不跟随
锁文件固定写死在`%APPDATA%\ClassRandomSampling\app.lock`，**不会跟随自定义data_dir移动**。
场景：用户切换到D盘自定义数据目录，同时开第二个程序指向默认目录，两套数据隔离但不会互斥，双实例并发读写同一份日志/json，文件错乱、数据覆盖丢失。
单实例锁只限制默认目录，自定义目录无单实例保护。

### 2. 切换数据目录时复制逻辑无文件占用捕获，文件被占用直接复制失败
`_switch_data_dir`中`shutil.copy2`、`copytree`无try单文件捕获，若当日日志正在被程序占用、json打开，整个复制流程直接中断，新旧目录数据不一致，一半文件复制成功一半失败。

### 3. 切换目录成功后，当日日志时间戳不刷新，新旧日志混写
切换目录后仅更新`data_dir`，但`current_log_path`是切换前当日日志路径；切换完成后继续往旧目录写抽取日志，新目录无抽取记录，日志分散两处。

### 4. 导入CSV失败无回滚，半脏数据残留内存
CSV解析中途报错时`self.students`不会复原为旧名单，内存里残留部分解析数据，状态栏、表格展示脏数据，重启才恢复。

## 二、高优先级：数据丢失风险Bug
### 1. 窗口关闭 `_save_data(backup=False)` 不生成备份
仅手动修改权重、导入CSV时才生成`.bak`；单纯浏览后关闭窗口，本次临时修改无备份，断电/系统崩溃无法恢复。

### 2. 日志满1000条静默删除最早日志，无弹窗、无日志记录删除行为
教师抽取记录无声丢失，无900条临近预警，无二次确认弹窗。

### 3. 多Windows账户可同时运行程序，日志/json无读写锁
不同用户隔离，双进程同时追加日志、写入class_data.json，出现文件截断、内容穿插、json结构损坏。

### 4. 日志写入失败仅3秒红色状态栏提示，自动消失无留存
磁盘满/目录只读时抽取记录丢失，用户极易错过提示，完全无持久告警弹窗。

### 5. CSV仅支持`utf-8-sig`，Excel默认GBK导出直接乱码解析失败
无编码自动探测、兼容降级逻辑，机房Excel导出名册无法正常导入。

## 三、中优先级：UI/逻辑混淆缺陷
### 1. 权重弹窗永远加载全部学生，不跟随当前性别筛选视图
勾选只抽女生打开弹窗，展示全班人员，用户误以为仅修改筛选内学生，极易误调整无关人员权重（学号匹配不会错乱，但交互误导极强）。

### 2. 表格正常展示权重=0学生，抽取自动过滤，无视觉灰色标记
用户看到名单里存在某人，但永远抽不到，界面无任何区分标识，新手会认为抽取功能失效。

### 3. 筛选后仅1名可抽取学生，无弹窗提示结果固定
无友好提示，用户误以为随机算法故障。

### 4. 权重修正弹窗最多展示前10条，完整修正清单**不写入操作日志**
批量导入大量异常权重，无法追溯全部自动调整记录，仅弹窗临时查看。

### 5. 无一键清空抽取结果按钮，只能切换筛选重置顶部文字
操作繁琐。

### 6. 日志文本框无导出、清空功能
教师无法导出当日抽取台账，只能手动去文件夹复制日志。

## 四、虚拟机极限测试专属隐患（机房低配置/权限/磁盘场景）
### 场景1：虚拟机普通用户，默认APPDATA目录只读
程序启动`_check_data_dir`写入测试文件失败，直接弹窗致命错误退出，完全无法启动；只能手动切换到D盘/桌面自定义目录。

### 场景2：磁盘空间耗尽
1. 保存json、备份bak失败，修改全部丢失；
2. 日志写入失败，抽取记录无留存；
3. 日志达到上限删除旧日志失败，持续循环遍历编号，轻微CPU占用升高。

### 场景3：强制杀进程/断电关闭
1. 关闭不生成bak备份，内存修改丢失；
2. json写入中途截断，下次启动依赖旧bak恢复；
3. QLockFile文件锁不会残留（已修复旧版QSharedMemory问题）。

### 场景4：400人大班超大名单
每次切换筛选、刷新状态栏全量循环计算总人数、男女、总权重，无缓存，低配置虚拟机UI明显卡顿；权重弹窗一次性渲染几百个SpinBox，弹窗打开延迟。

### 场景5：多班级交替导入
无多班级切换菜单，只能覆盖单份class_data.json，旧班级数据只能通过切换数据目录分离，操作门槛极高。

## 五、代码设计短板
1. 全部文案、模式字符串、文件后缀硬编码分散，无统一常量；
2. 学生使用原生dict裸访问，无数据模型封装，易漏字段处理；
3. 男女数量、总权重无缓存，每次刷新全量遍历；
4. 缺少菜单：导出日志、手动备份名册、批量权重操作（批量置0/统一权重）；
5. 无窗口大小/位置记忆，每次启动恢复默认尺寸；
6. 关于窗口文本不会自适应换行，缩小后文字挤压重叠。

## 六、本次版本对比v2.1 已修复优化点
✅ 替换QSharedMemory为QLockFile，程序崩溃不再残留实例锁，虚拟机杀进程后可正常重启；
✅ 新增自定义数据目录功能，机房APPDATA受限用户可转移到D盘/桌面；
✅ 使用QSettings持久化自定义目录，重启自动加载；
✅ 抽取结果同时展示「三位学号+姓名」，信息更完整；
✅ 锁文件独立，单实例可靠性大幅提升；
✅ 回归JSON轻量化存储，移除SQLite先删后插清空数据的致命风险。

## 七、优先级修复清单（虚拟机机房必改）
1. 将QLockFile锁文件路径跟随当前data_dir，多目录隔离单实例；
2. 切换数据目录后刷新当日日志路径，新旧目录日志分离；
3. CSV增加GBK/UTF-8自动编码探测；
4. 表格中weight=0学生文字置灰，视觉区分不可抽取人员；
5. 日志达到900条提前弹窗预警，删除旧日志前二次确认；
6. 关闭窗口统一生成bak备份，避免断电丢失修改；
7. 权重修正完整记录写入操作日志，不截断；
8. 增加缓存变量缓存学生统计数据，减少重复遍历卡顿；
9. 新增菜单导出抽取日志、一键清空日志显示；
10. 筛选仅1人抽取时弹出提示告知结果固定。